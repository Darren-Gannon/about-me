import { YearsSincePipe } from './years-since.pipe';

describe('YearsSincePipe', () => {
  it('create an instance', () => {
    const pipe = new YearsSincePipe();
    expect(pipe).toBeTruthy();
  });
});
